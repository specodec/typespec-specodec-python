# Generated by @specodec/typespec-emitter-python. DO NOT EDIT.
from __future__ import annotations
import enum
from dataclasses import dataclass
from typing import Optional, Any, Callable, List, Union
from specodec import SpecWriter, SpecReader, SpecCodec, SpecUndefined

@dataclass
class Inner:
    x: int
    y: int

@dataclass
class Coord:
    lat: float
    lng: float

@dataclass
class IdVal:
    id: str
    value: int

@dataclass
class Label:
    key: str
    text: str

@dataclass
class Money:
    amount: int
    currency: str

@dataclass
class Range32:
    min: int
    max: int

@dataclass
class Addr:
    street: str
    city: str
    zip: str

@dataclass
class Point3:
    x: float
    y: float
    z: float

@dataclass
class OptInner:
    name: Optional[str] = None
    score: Optional[float] = None
    tag: Optional[str] = None

def write_inner(w: SpecWriter, obj: Inner) -> None:
    w.begin_object(2)
    w.write_field("x")
    w.write_int32(int(obj.x))
    w.write_field("y")
    w.write_int32(int(obj.y))
    w.end_object()

def decode_inner(r: SpecReader) -> Inner:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "x": kw["x"] = r.read_int32(); continue
        if key == "y": kw["y"] = r.read_int32(); continue
        r.skip()
    r.end_object()
    return Inner(**kw)

def write_coord(w: SpecWriter, obj: Coord) -> None:
    w.begin_object(2)
    w.write_field("lat")
    w.write_float64(float(obj.lat))
    w.write_field("lng")
    w.write_float64(float(obj.lng))
    w.end_object()

def decode_coord(r: SpecReader) -> Coord:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "lat": kw["lat"] = r.read_float64(); continue
        if key == "lng": kw["lng"] = r.read_float64(); continue
        r.skip()
    r.end_object()
    return Coord(**kw)

def write_id_val(w: SpecWriter, obj: IdVal) -> None:
    w.begin_object(2)
    w.write_field("id")
    w.write_string(obj.id)
    w.write_field("value")
    w.write_int32(int(obj.value))
    w.end_object()

def decode_id_val(r: SpecReader) -> IdVal:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "id": kw["id"] = r.read_string(); continue
        if key == "value": kw["value"] = r.read_int32(); continue
        r.skip()
    r.end_object()
    return IdVal(**kw)

def write_label(w: SpecWriter, obj: Label) -> None:
    w.begin_object(2)
    w.write_field("key")
    w.write_string(obj.key)
    w.write_field("text")
    w.write_string(obj.text)
    w.end_object()

def decode_label(r: SpecReader) -> Label:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "key": kw["key"] = r.read_string(); continue
        if key == "text": kw["text"] = r.read_string(); continue
        r.skip()
    r.end_object()
    return Label(**kw)

def write_money(w: SpecWriter, obj: Money) -> None:
    w.begin_object(2)
    w.write_field("amount")
    w.write_int64(int(obj.amount))
    w.write_field("currency")
    w.write_string(obj.currency)
    w.end_object()

def decode_money(r: SpecReader) -> Money:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "amount": kw["amount"] = r.read_int64(); continue
        if key == "currency": kw["currency"] = r.read_string(); continue
        r.skip()
    r.end_object()
    return Money(**kw)

def write_range32(w: SpecWriter, obj: Range32) -> None:
    w.begin_object(2)
    w.write_field("min")
    w.write_int32(int(obj.min))
    w.write_field("max")
    w.write_int32(int(obj.max))
    w.end_object()

def decode_range32(r: SpecReader) -> Range32:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "min": kw["min"] = r.read_int32(); continue
        if key == "max": kw["max"] = r.read_int32(); continue
        r.skip()
    r.end_object()
    return Range32(**kw)

def write_addr(w: SpecWriter, obj: Addr) -> None:
    w.begin_object(3)
    w.write_field("street")
    w.write_string(obj.street)
    w.write_field("city")
    w.write_string(obj.city)
    w.write_field("zip")
    w.write_string(obj.zip)
    w.end_object()

def decode_addr(r: SpecReader) -> Addr:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "street": kw["street"] = r.read_string(); continue
        if key == "city": kw["city"] = r.read_string(); continue
        if key == "zip": kw["zip"] = r.read_string(); continue
        r.skip()
    r.end_object()
    return Addr(**kw)

def write_point3(w: SpecWriter, obj: Point3) -> None:
    w.begin_object(3)
    w.write_field("x")
    w.write_float64(float(obj.x))
    w.write_field("y")
    w.write_float64(float(obj.y))
    w.write_field("z")
    w.write_float64(float(obj.z))
    w.end_object()

def decode_point3(r: SpecReader) -> Point3:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "x": kw["x"] = r.read_float64(); continue
        if key == "y": kw["y"] = r.read_float64(); continue
        if key == "z": kw["z"] = r.read_float64(); continue
        r.skip()
    r.end_object()
    return Point3(**kw)

def write_opt_inner(w: SpecWriter, obj: OptInner) -> None:
    field_count = 0
    if obj.name is not None: field_count += 1
    if obj.score is not None: field_count += 1
    if obj.tag is not None: field_count += 1
    w.begin_object(field_count)
    if obj.name is not None:
        w.write_field("name")
        w.write_string(obj.name)
    if obj.score is not None:
        w.write_field("score")
        w.write_float64(float(obj.score))
    if obj.tag is not None:
        w.write_field("tag")
        w.write_string(obj.tag)
    w.end_object()

def decode_opt_inner(r: SpecReader) -> OptInner:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "name": kw["name"] = r.read_string(); continue
        if key == "score": kw["score"] = r.read_float64(); continue
        if key == "tag": kw["tag"] = r.read_string(); continue
        r.skip()
    r.end_object()
    return OptInner(**kw)

InnerCodec: SpecCodec = SpecCodec(
    encode=write_inner,
    decode=decode_inner,
)

CoordCodec: SpecCodec = SpecCodec(
    encode=write_coord,
    decode=decode_coord,
)

IdValCodec: SpecCodec = SpecCodec(
    encode=write_id_val,
    decode=decode_id_val,
)

LabelCodec: SpecCodec = SpecCodec(
    encode=write_label,
    decode=decode_label,
)

MoneyCodec: SpecCodec = SpecCodec(
    encode=write_money,
    decode=decode_money,
)

Range32Codec: SpecCodec = SpecCodec(
    encode=write_range32,
    decode=decode_range32,
)

AddrCodec: SpecCodec = SpecCodec(
    encode=write_addr,
    decode=decode_addr,
)

Point3Codec: SpecCodec = SpecCodec(
    encode=write_point3,
    decode=decode_point3,
)

OptInnerCodec: SpecCodec = SpecCodec(
    encode=write_opt_inner,
    decode=decode_opt_inner,
)
