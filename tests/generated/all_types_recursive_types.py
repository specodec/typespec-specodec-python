# Generated by @specodec/typespec-emitter-python. DO NOT EDIT.
from __future__ import annotations
import enum
from dataclasses import dataclass
from typing import Optional, Any, Callable, List, Union
from specodec import SpecWriter, SpecReader, SpecCodec, SpecUndefined

@dataclass
class RecList:
    value: int
    next: Optional[RecList] = None

@dataclass
class RecTree:
    value: str
    left_node: Optional[RecTree] = None
    right_node: Optional[RecTree] = None

@dataclass
class RecChain:
    id: int
    label: str
    next: Optional[RecChain] = None

@dataclass
class RecWrap:
    payload: bytes
    nested: Optional[RecWrap] = None

@dataclass
class RecWide:
    a: int
    b: str
    c: float
    child: Optional[RecWide] = None

def write_rec_list(w: SpecWriter, obj: RecList) -> None:
    field_count = 1
    if obj.next is not None: field_count += 1
    w.begin_object(field_count)
    w.write_field("value")
    w.write_int32(int(obj.value))
    if obj.next is not None:
        w.write_field("next")
        write_rec_list(w, obj.next)
    w.end_object()

def decode_rec_list(r: SpecReader) -> RecList:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "value": kw["value"] = r.read_int32(); continue
        if key == "next":
            tmp0 = None
            if r.is_null():
                r.read_null()
            else:
                tmp0 = decode_rec_list(r)
            kw["next"] = tmp0; continue
        r.skip()
    r.end_object()
    return RecList(**kw)

def write_rec_tree(w: SpecWriter, obj: RecTree) -> None:
    field_count = 1
    if obj.left_node is not None: field_count += 1
    if obj.right_node is not None: field_count += 1
    w.begin_object(field_count)
    w.write_field("value")
    w.write_string(obj.value)
    if obj.left_node is not None:
        w.write_field("left_node")
        write_rec_tree(w, obj.left_node)
    if obj.right_node is not None:
        w.write_field("right_node")
        write_rec_tree(w, obj.right_node)
    w.end_object()

def decode_rec_tree(r: SpecReader) -> RecTree:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "value": kw["value"] = r.read_string(); continue
        if key == "left_node":
            tmp0 = None
            if r.is_null():
                r.read_null()
            else:
                tmp0 = decode_rec_tree(r)
            kw["left_node"] = tmp0; continue
        if key == "right_node":
            tmp1 = None
            if r.is_null():
                r.read_null()
            else:
                tmp1 = decode_rec_tree(r)
            kw["right_node"] = tmp1; continue
        r.skip()
    r.end_object()
    return RecTree(**kw)

def write_rec_chain(w: SpecWriter, obj: RecChain) -> None:
    field_count = 2
    if obj.next is not None: field_count += 1
    w.begin_object(field_count)
    w.write_field("id")
    w.write_int32(int(obj.id))
    w.write_field("label")
    w.write_string(obj.label)
    if obj.next is not None:
        w.write_field("next")
        write_rec_chain(w, obj.next)
    w.end_object()

def decode_rec_chain(r: SpecReader) -> RecChain:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "id": kw["id"] = r.read_int32(); continue
        if key == "label": kw["label"] = r.read_string(); continue
        if key == "next":
            tmp0 = None
            if r.is_null():
                r.read_null()
            else:
                tmp0 = decode_rec_chain(r)
            kw["next"] = tmp0; continue
        r.skip()
    r.end_object()
    return RecChain(**kw)

def write_rec_wrap(w: SpecWriter, obj: RecWrap) -> None:
    field_count = 1
    if obj.nested is not None: field_count += 1
    w.begin_object(field_count)
    w.write_field("payload")
    w.write_bytes(obj.payload)
    if obj.nested is not None:
        w.write_field("nested")
        write_rec_wrap(w, obj.nested)
    w.end_object()

def decode_rec_wrap(r: SpecReader) -> RecWrap:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "payload": kw["payload"] = r.read_bytes(); continue
        if key == "nested":
            tmp0 = None
            if r.is_null():
                r.read_null()
            else:
                tmp0 = decode_rec_wrap(r)
            kw["nested"] = tmp0; continue
        r.skip()
    r.end_object()
    return RecWrap(**kw)

def write_rec_wide(w: SpecWriter, obj: RecWide) -> None:
    field_count = 3
    if obj.child is not None: field_count += 1
    w.begin_object(field_count)
    w.write_field("a")
    w.write_int32(int(obj.a))
    w.write_field("b")
    w.write_string(obj.b)
    w.write_field("c")
    w.write_float64(float(obj.c))
    if obj.child is not None:
        w.write_field("child")
        write_rec_wide(w, obj.child)
    w.end_object()

def decode_rec_wide(r: SpecReader) -> RecWide:
    kw: dict = {}
    r.begin_object()
    while r.has_next_field():
        key = r.read_field_name()
        if key == "a": kw["a"] = r.read_int32(); continue
        if key == "b": kw["b"] = r.read_string(); continue
        if key == "c": kw["c"] = r.read_float64(); continue
        if key == "child":
            tmp0 = None
            if r.is_null():
                r.read_null()
            else:
                tmp0 = decode_rec_wide(r)
            kw["child"] = tmp0; continue
        r.skip()
    r.end_object()
    return RecWide(**kw)

RecListCodec: SpecCodec = SpecCodec(
    encode=write_rec_list,
    decode=decode_rec_list,
)

RecTreeCodec: SpecCodec = SpecCodec(
    encode=write_rec_tree,
    decode=decode_rec_tree,
)

RecChainCodec: SpecCodec = SpecCodec(
    encode=write_rec_chain,
    decode=decode_rec_chain,
)

RecWrapCodec: SpecCodec = SpecCodec(
    encode=write_rec_wrap,
    decode=decode_rec_wrap,
)

RecWideCodec: SpecCodec = SpecCodec(
    encode=write_rec_wide,
    decode=decode_rec_wide,
)
